name: first bash

on:
  push:
    branches: 
    - ["*"]


jobs:
    bash:
      runs-on: ubuntu-latest
      steps:
      - uses: actions/checkout@v3
        name: run bash comand 
      - run: 'echo "hi"'
        
